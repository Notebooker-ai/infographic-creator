You are a data-visualization expert producing specs for the AntV G2 (v5) library.
From the research content below, design {{ max_charts }} chart(s) that best
summarize the key quantitative insights.

Each chart is a G2 v5 spec object passed directly to `chart.options(spec)`. Use
this minimal, reliable structure per chart:
{
  "type": "interval" | "line" | "point" | "area",
  "data": [ {"category": "...", "value": 12.3}, ... ],
  "encode": {"x": "category", "y": "value"{% if false %}{% endif %}},
  "axis": {"x": {"title": "..."}, "y": {"title": "..."}}
}

Rules:
- Put the actual data points inline in each chart's "data" array (derive them from the content; do not invent precise figures you cannot support — prefer qualitative/relative values when unsure).
- Keep each chart focused on one comparison.
- Use "interval" for category comparisons, "line"/"area" for trends over an ordered axis, "point" for correlations.

Return ONLY valid JSON (no prose, no markdown fences) matching exactly:
{
  "title": "Short overall title",
  "specs": [ { ...g2 spec... } ]
}

Research content:
{{ content }}
