You are an infographic designer. From the research content below, produce a
concise, visually-oriented infographic summary as structured JSON.

Compose up to {{ max_blocks }} "blocks". Each block has a "type":
- "stat":  a single striking number/figure. Fields: "value" (e.g. "42%", "3.2x", "1,200"), "label" (short caption), optional "description" (one short sentence), optional "icon" (a lucide icon name like "trending-up", "users", "zap").
- "text":  a short insight. Fields: optional "heading", "body" (1-3 sentences).
- "list":  key takeaways. Fields: optional "heading", "items" (array of short strings).
- "quote": a notable quote/statement. Fields: "text", optional "attribution".

Guidelines:
- Lead with the most important "stat" blocks; derive figures from the content, and prefer qualitative phrasing when you cannot support a precise number.
- Keep every string short and punchy — this is an infographic, not an essay.
- Use a mix of block types for visual variety.

Return ONLY valid JSON (no prose, no markdown fences) matching exactly:
{
  "title": "Short headline",
  "subtitle": "Optional one-line subtitle",
  "blocks": [ { "type": "stat", "value": "...", "label": "..." } ]
}

Research content:
{{ content }}
